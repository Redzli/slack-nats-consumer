apiVersion: v1
kind: Service
metadata:
  name: {{ include "template-nats-consumer.fullname" . }}
  labels:
    {{- include "template-nats-consumer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "template-nats-consumer.selectorLabels" . | nindent 4 }}
